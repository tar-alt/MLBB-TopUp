{
  "name": "mlbb-topup",
  "version": "2.1.0",
  "private": true,
  "description": "MLBB top-up storefront with SQLite order tracking and admin workflow",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node db/migrate.js",
    "check": "node --check server.js && node --check db/migrate.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.7"
  }
}
